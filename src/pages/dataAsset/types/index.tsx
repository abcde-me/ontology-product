/** 导入类型 */
export enum ImportType {
  append = 'append',
  overwrite = 'overwrite'
}

/** 文件上传状态 */
export enum UploadStatus {
  init = 'init',
  done = 'done',
  uploading = 'uploading',
  error = 'error'
}
