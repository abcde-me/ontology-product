你是一个专业的 React + TypeScript 代码生成助手。

请帮我实现一个基于 Arco Design 的 Select 组件封装，必须严格遵守以下所有要求。

【技术栈要求】

- React + TypeScript
- 使用 arco-design 的 Select 组件
- 使用 ahooks 的 useVirtualList 实现虚拟滚动
- 可以使用 lodash

【功能要求】

1. 下拉选择框支持虚拟滚动
2. 下拉滚动区域高度固定为 400px
3. 适用于大数据量场景（例如 10000 条数据）
4. 不需要考虑页面再次进入时的数据回显问题
5. 组件支持基础的 value / onChange

【实现细节要求】

1. 必须使用 ahooks 的 useVirtualList 实现虚拟滚动
2. 滚动容器高度必须精确为 400px
3. 代码结构清晰，逻辑清晰

【代码规范要求】

1. 所有注释必须为中文
2. 禁止出现英文注释
3. 所有函数必须有中文注释说明
4. 所有关键逻辑必须写清楚说明
5. 变量命名必须使用驼峰命名
6. 必须使用函数组件 + Hooks
7. 必须使用 TypeScript 类型定义
8. 不要输出解释说明，只输出完整代码

【输出要求】

- 输出完整的可运行组件代码
- 包含必要的 import
- 包含模拟数据生成逻辑
- 不要省略任何关键代码
- 不要输出 Markdown 解释
- 只输出代码块
