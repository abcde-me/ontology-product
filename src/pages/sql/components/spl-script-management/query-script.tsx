import React, { memo } from 'react';

import styles from './query-script.module.scss';

const QueryScript: React.FC = memo(() => {
  return <div className={styles['query-script']}>2</div>;
});

export default QueryScript;
